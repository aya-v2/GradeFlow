<form method="post" action="{{ path('app_assessment_delete', {'id': assessment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assessment.id) }}">
    <button class="btn">Delete</button>
</form>
